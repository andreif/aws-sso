# aws-sso

```shell
# Download the script
curl -O https://raw.githubusercontent.com/andreif/aws-sso/main/aws-sso.py
# Only allow your current user to access it
chmod 0700 aws-sso.py
# Move it to other executables so it can be run as `aws-sso ...`
sudo mv aws-sso.py /usr/local/bin/aws-sso
```
